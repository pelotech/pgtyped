lib
node_modules
pnpm-lock.yaml

# Generated by pgtyped's own codegen, which does not run prettier. Formatting
# these would make them differ from what a fresh run emits.
**/*.queries.ts
**/*.types.ts

# The docs site is not part of the library build and is left to its own tooling.
docs-new
