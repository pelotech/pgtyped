{
  "name": "@pelotech/pgtyped-example",
  "version": "2.4.3",
  "description": "A package demonstrating how pgtyped works.",
  "author": "Adel Salakh",
  "private": true,
  "homepage": "https://github.com/pelotech/pgtyped",
  "license": "MIT",
  "exports": "./lib/index.js",
  "type": "module",
  "directories": {
    "lib": "lib"
  },
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "docker compose run build && docker compose run test",
    "typegen": "pgtyped -c config.json",
    "build": "echo 'No build step required. Use pnpm test instead'",
    "watch": "echo 'No build step required. Use pnpm test instead'",
    "check": "tsc --noEmit",
    "check:test": "pnpm run check"
  },
  "dependencies": {
    "@pelotech/pgtyped-cli": "workspace:^",
    "@pelotech/pgtyped-runtime": "workspace:^",
    "pg": "catalog:",
    "typescript": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@types/pg": "catalog:",
    "ts-node": "10.9.2",
    "vitest": "catalog:"
  }
}
